import { lazy, Suspense, useEffect, useMemo, useState } from "react";
import { AlertTriangle, CircleHelp, Compass, Eye, EyeOff, Gauge, Keyboard, Menu, Move3D, PanelLeftClose, Sparkles, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Switch } from "@/components/ui/switch";
import { trpc } from "@/lib/trpc";
import { WorldContext } from "./WorldContext";
import { roomNames, type AriaState, type RoomId, type VaultObjectId } from "./types";
import { canEnterWorldRoom, getChamberGuidance, getWorldSignal, shouldEnterPersistentWorld } from "./worldExperience";

const VaultScene = lazy(() => import("./VaultScene"));
function useSmallViewport() { const [small, setSmall] = useState(() => typeof window !== "undefined" && window.matchMedia("(max-width: 760px)").matches); useEffect(() => { const media = window.matchMedia("(max-width: 760px)"); const listener = () => setSmall(media.matches); listener(); media.addEventListener("change", listener); return () => media.removeEventListener("change", listener); }, []); return small; }

export default function VaultExperience() {
  const utils = trpc.useUtils(); const stateQuery = trpc.vault.state.useQuery();
  const [entered, setEntered] = useState(false); const [room, setRoom] = useState<RoomId>("central-chamber"); const [showDeck, setShowDeck] = useState(false); const [announcement, setAnnouncement] = useState<string | null>(null); const [webglFailure, setWebglFailure] = useState(false); const [ariaState, setAriaState] = useState<AriaState>("idle"); const [selectedArtifactId, setSelectedArtifactId] = useState<string | null>(null); const [catalogOpen, setCatalogOpen] = useState(false); const [ariaFocused, setAriaFocused] = useState(false); const smallViewport = useSmallViewport();
  const enterRoom = trpc.vault.enterRoom.useMutation({ onSuccess: () => utils.vault.state.invalidate(), onError: error => setAnnouncement(error.message) });
  const discover = trpc.vault.discover.useMutation({ onSuccess: data => { utils.vault.state.invalidate(); if (data.isNew) setAnnouncement(`DISCOVERY RETAINED — ${data.object.name}${data.unlockedRoomId ? ` · ${roomNames[data.unlockedRoomId as RoomId]} is now open` : ""}`); }, onError: error => setAnnouncement(error.message) });
  const settings = trpc.vault.settings.useMutation({ onSuccess: () => utils.vault.state.invalidate(), onError: error => setAnnouncement(error.message) });
  const note = trpc.vault.saveNote.useMutation({ onSuccess: () => { utils.vault.state.invalidate(); setAnnouncement("EXPERIMENT PRESERVED — A new trace has been placed in the Archive."); }, onError: error => setAnnouncement(error.message) });
  const materialize = trpc.vault.materialize.useMutation({ onSuccess: data => { utils.vault.state.invalidate(); setAnnouncement(`MATERIALIZED — ${data?.title ?? "Your experiment"} has become an Archive object.`); }, onError: error => setAnnouncement(error.message) });
  const understand = trpc.vault.understand.useMutation({ onSuccess: data => { utils.vault.state.invalidate(); setAnnouncement(data.unlockedObjectId ? "RELATIONSHIP UNDERSTOOD — The Resonance Needle has appeared." : "RELATIONSHIP UNDERSTOOD — A deeper route is visible."); }, onError: error => setAnnouncement(error.message) });
  const messages = trpc.aria.messages.useQuery(undefined, { enabled: room === "lab" && ariaFocused });
  const aria = trpc.aria.send.useMutation({ onMutate: () => setAriaState("thinking"), onSuccess: data => { utils.aria.messages.invalidate(); utils.vault.state.invalidate(); setAriaState("responding"); applyAriaAction(data.suggestedAction); window.setTimeout(() => setAriaState("success"), 300); window.setTimeout(() => setAriaState("idle"), 1500); }, onError: error => { setAriaState("error"); setAnnouncement(error.message); window.setTimeout(() => setAriaState("idle"), 1800); } });
  const state = stateQuery.data; const reducedMotion = state?.settings?.reducedMotion ?? false; const preferFallback = state?.settings?.preferFallback ?? false; const renderQuality = state?.settings?.renderQuality ?? "auto"; const lowPower = preferFallback || reducedMotion || renderQuality === "low" || (renderQuality === "auto" && smallViewport); const isInWorld = shouldEnterPersistentWorld(entered, state?.settings?.introSeen === true);
  const unlockedRooms = useMemo(() => state?.roomStates.filter(item => item.isUnlocked).map(item => item.roomId) ?? ["central-chamber"], [state?.roomStates]); const discoveredObjects = useMemo(() => state?.discoveries.map(item => item.objectId) ?? [], [state?.discoveries]); const objectStates = useMemo(() => new Map(state?.objectStates.map(item => [item.objectId, item.state]) ?? []), [state?.objectStates]); const sceneObjectStates = useMemo(() => Object.fromEntries(objectStates), [objectStates]);
  const archiveArtifacts = useMemo(() => { const discovered = new Set(discoveredObjects); return (state?.artifacts ?? []).filter(item => discovered.has(item.objectId)).map(item => ({ id: item.id, objectId: item.objectId, title: item.title, category: item.category, accent: item.accent })); }, [state?.artifacts, discoveredObjects]);
  const worldSignal = getWorldSignal(sceneObjectStates); const isReturnVisit = (state?.history ?? []).filter(entry => entry.eventType === "visit" && entry.targetId === "central-chamber").length > 1; const chamberGuidance = getChamberGuidance(worldSignal, isReturnVisit); const visibleAriaState: AriaState = ariaState === "idle" && room === "lab" ? "listening" : ariaState;
  useEffect(() => { const canvas = document.createElement("canvas"); setWebglFailure(!Boolean(canvas.getContext("webgl2") || canvas.getContext("webgl"))); }, []); useEffect(() => { const lastRoom = state?.settings?.lastRoomId; if (["archive", "lab", "observatory"].includes(lastRoom ?? "")) setRoom(lastRoom as RoomId); }, [state?.settings?.lastRoomId]); useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.key === "Escape") { setSelectedArtifactId(null); setCatalogOpen(false); setAriaFocused(false); setShowDeck(false); } if (event.key.toLowerCase() === "m") setShowDeck(value => !value); }; window.addEventListener("keydown", onKey); return () => window.removeEventListener("keydown", onKey); }, []);
  const changeRoom = (nextRoom: RoomId) => { if (!canEnterWorldRoom(nextRoom, unlockedRooms)) { setAriaState("warning"); setAnnouncement("This threshold is sealed. Its clue is somewhere in the chamber."); window.setTimeout(() => setAriaState("idle"), 1600); return; } setRoom(nextRoom); setEntered(true); setSelectedArtifactId(null); setCatalogOpen(false); setAriaFocused(false); enterRoom.mutate({ roomId: nextRoom }); };
  const applyAriaAction = (action: "none" | "navigate_archive" | "navigate_lab" | "navigate_observatory" | "show_discoveries") => { if (action === "show_discoveries") { setAnnouncement("ARIA has indicated the recorded objects in THE ARCHIVE."); changeRoom("archive"); return; } if (action.startsWith("navigate_")) { const next = action.replace("navigate_", "") as RoomId; setAnnouncement(`ARIA has opened a route to ${roomNames[next]}.`); changeRoom(next); } };
  const activateObject = (objectId: VaultObjectId) => { if (objectId === "door-archive") return changeRoom("archive"); if (objectId === "door-lab") return changeRoom("lab"); if (objectId === "aria-entity") { changeRoom("lab"); setAriaFocused(true); return; } if (objectId === "door-observatory") return changeRoom("observatory"); discover.mutate({ objectId }); };
  const start = () => { setEntered(true); settings.mutate({ introSeen: true }); enterRoom.mutate({ roomId: "central-chamber" }); };
  if (stateQuery.isLoading) return <div className="vault-loading"><div className="loading-sigil"><span /></div><p>THE VAULT IS REMEMBERING YOU</p></div>;
  if (stateQuery.error || !state) return <div className="vault-loading vault-error"><AlertTriangle /><h1>The Vault could not be opened.</h1><p>Your discoveries remain safe. Please refresh, then try again.</p><Button onClick={() => stateQuery.refetch()}>Try again</Button></div>;
  return <main className={`vault-app ${state.settings?.highContrast ? "vault-app--contrast" : ""} ${reducedMotion ? "vault-app--reduced" : ""}`}><div className="vault-scene" aria-hidden="true">{!preferFallback && !webglFailure && <Suspense fallback={<div className="scene-skeleton" />}><VaultScene room={room} discoveredObjectIds={discoveredObjects} unlockedRoomIds={unlockedRooms} reducedMotion={reducedMotion} lowPower={lowPower} renderQuality={renderQuality} ariaState={visibleAriaState} objectStates={sceneObjectStates} worldSignal={worldSignal} isReturnVisit={isReturnVisit} archiveArtifacts={archiveArtifacts} onActivate={activateObject} onInspectArtifact={setSelectedArtifactId} onToggleCatalog={() => setCatalogOpen(value => !value)} onFocusAria={() => setAriaFocused(true)} onExitRoom={() => changeRoom("central-chamber")} /></Suspense>}</div>
    {(preferFallback || webglFailure) && <section className="fallback-chamber" aria-label="THE VAULT accessible spatial interface"><span className="eyebrow">{webglFailure ? "WEBGL UNAVAILABLE — ACCESSIBLE MODE ACTIVE" : "NON-3D MODE ACTIVE"}</span><h2>{room === "central-chamber" ? "THE VAULT'S CENTRAL CHAMBER" : roomNames[room]}</h2><p>{room === "central-chamber" ? "The same thresholds remain. Choose a route or inspect the unresolved signal." : "This room is open. Its records remain available here."}</p>{room === "central-chamber" && <div className="fallback-thresholds"><button onClick={() => changeRoom("archive")}><Eye /><span>THRESHOLD</span>THE ARCHIVE</button><button onClick={() => changeRoom("lab")}><Sparkles /><span>THRESHOLD</span>THE LAB</button><button disabled={!unlockedRooms.includes("observatory")} onClick={() => changeRoom("observatory")}><Move3D /><span>{unlockedRooms.includes("observatory") ? "THRESHOLD" : "SEALED"}</span>THE OBSERVATORY</button><button onClick={() => activateObject("object-memory-prism")}><CircleHelp /><span>{discoveredObjects.includes("object-memory-prism") ? "RECORDED" : "UNRESOLVED"}</span>MEMORY PRISM</button></div>}</section>}
    {!isInWorld && <section className="entry-sequence" aria-label="Enter THE VAULT"><div className="entry-sequence__signal"><span /></div><p className="eyebrow">A PERSISTENT WORLD OF ATTENTION</p><h1>THE<br />VAULT</h1><p className="entry-sequence__copy">A chamber of routes, objects, and consequences.</p><Button className="enter-button" onClick={start}>Cross the threshold</Button><p className="entry-sequence__hint">Explore with mouse, touch, keyboard, or the accessible route deck.</p></section>}
    {isInWorld && <><div className="world-hud"><span className="world-hud__room">{room === "central-chamber" ? "CHAMBER" : roomNames[room]}</span><button onClick={() => setShowDeck(value => !value)} aria-expanded={showDeck} aria-controls="access-deck">{showDeck ? <X /> : <Menu />}<span className="sr-only">Open access deck</span></button></div><div className="scene-prompt"><span>{room === "central-chamber" ? chamberGuidance : room === "archive" ? "The shelves respond to objects you have retained." : room === "lab" ? "ARIA is present in the containment cradle." : "Your history is held as a constellation."}</span></div>{room === "central-chamber" && <div className="mobile-vectors" aria-label="Spatial touch controls"><button onClick={() => worldSignal === "resonant" ? activateObject("object-echo-sigil") : worldSignal === "mastered" ? changeRoom("observatory") : activateObject("object-memory-prism")}><Sparkles /><span>{worldSignal === "resonant" ? "Follow the Echo" : worldSignal === "mastered" ? "Trace the Observatory" : "Inspect the Prism"}</span></button><button onClick={() => { changeRoom("lab"); setAriaFocused(true); }}><CircleHelp /><span>Approach ARIA</span></button><button onClick={() => setShowDeck(true)}><Menu /><span>Routes</span></button></div>}<WorldContext room={room} state={state} selectedArtifactId={selectedArtifactId} catalogOpen={catalogOpen} ariaFocused={ariaFocused} messages={(messages.data ?? []) as never} ariaState={visibleAriaState} sending={aria.isPending} saving={note.isPending} materializing={materialize.isPending ? materialize.variables?.noteId ?? null : null} understanding={understand.isPending} onCloseArtifact={() => setSelectedArtifactId(null)} onCloseCatalog={() => setCatalogOpen(false)} onCloseAria={() => setAriaFocused(false)} onInspectArtifact={setSelectedArtifactId} onSend={message => aria.mutate({ roomId: "lab", message })} onSaveNote={(title, content) => note.mutate({ title, content })} onMaterialize={noteId => materialize.mutate({ noteId})} onUnderstand={objectId => understand.mutate({ objectId})} /><aside id="access-deck" className={`access-deck ${showDeck ? "access-deck--open" : ""}`} aria-label="Accessible Vault navigation"><div className="access-deck__head"><div><span className="eyebrow">ACCESS DECK</span><strong>Routes and environment.</strong></div><Button variant="ghost" size="icon" onClick={() => setShowDeck(false)}><PanelLeftClose /></Button></div><nav aria-label="Vault destinations"><button className={room === "central-chamber" ? "is-active" : ""} onClick={() => changeRoom("central-chamber")}><Compass /> The Vault's central chamber</button>{(["archive", "lab", "observatory"] as RoomId[]).map(destination => <button className={room === destination ? "is-active" : ""} disabled={!unlockedRooms.includes(destination)} key={destination} onClick={() => changeRoom(destination)}>{destination === "archive" ? <Eye /> : destination === "lab" ? <Sparkles /> : <Move3D />}{roomNames[destination]}{!unlockedRooms.includes(destination) && <span>SEALED</span>}</button>)}</nav><div className="access-deck__preferences"><span className="eyebrow">ENVIRONMENT</span><label><span><Gauge /> Reduced motion</span><Switch checked={reducedMotion} onCheckedChange={checked => settings.mutate({ reducedMotion: checked })} /></label><label><span><EyeOff /> High contrast</span><Switch checked={state.settings?.highContrast ?? false} onCheckedChange={checked => settings.mutate({ highContrast: checked })} /></label><label><span><PanelLeftClose /> Non-3D mode</span><Switch checked={preferFallback} onCheckedChange={checked => settings.mutate({ preferFallback: checked })} /></label><label className="quality-select"><span><Gauge /> Render quality</span><select aria-label="Rendering quality" value={renderQuality} onChange={event => settings.mutate({ renderQuality: event.target.value as "auto" | "high" | "low"})}><option value="auto">Auto</option><option value="high">High</option><option value="low">Low</option></select></label></div><div className="access-deck__keys"><Keyboard /><span><kbd>M</kbd> Routes <kbd>Esc</kbd> Dismiss context</span></div></aside><button className="access-deck-trigger" onClick={() => setShowDeck(true)}><CircleHelp /> <span>Accessible controls</span></button>{announcement && <div className="discovery-notice" role="status"><Sparkles /><div><span>VAULT SIGNAL</span><p>{announcement}</p></div><button onClick={() => setAnnouncement(null)} aria-label="Dismiss notification">×</button></div>}</>}
  </main>;
}
